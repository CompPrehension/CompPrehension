# all basic settings are located in server application.properties
# this file contains only test-specific overrides

# db
spring.datasource.url=jdbc:mysql://compprehension.ru/test_db2?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=True
spring.datasource.username=testdbuser
spring.datasource.password=*******
spring.liquibase.enabled=false

# for data.sql
spring.sql.init.mode=always
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# server-specific options
spring.security.enabled=false
