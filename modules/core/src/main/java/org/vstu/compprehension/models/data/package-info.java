/**
 * Модели данных, которые сервисы отдают бизнес-логике.
 * <p>
 * Отличаются от {@code org.vstu.compprehension.dto} тем, что те описывают контракт HTTP
 * и уходят в контроллеры, а эти — внутренние: их форма диктуется тем, что нужно
 * потребителю (например, стратегии), и меняется вместе с ним.
 * <p>
 * Все типы здесь immutable и полностью отсоединены от Hibernate: обращение к любому
 * их полю не выполняет запросов и не требует открытой сессии. Именно поэтому стратегии
 * работают с ними, а не с сущностями.
 */
package org.vstu.compprehension.models.data;
