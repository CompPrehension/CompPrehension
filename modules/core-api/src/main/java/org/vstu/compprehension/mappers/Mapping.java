package org.vstu.compprehension.mappers;

/**
 * Общий предок всего маппинга.
 * <p>
 * Ничего не объявляет и объявлять не должен: существует ради того, чтобы «маппер»
 * оставался типом, а не соглашением об именовании. На нём держатся правила из
 * {@code MapperConventionTest} — без него им пришлось бы опираться на суффикс класса,
 * а именные соглашения гниют первыми.
 * <p>
 * Наследуют его {@link Mapper} и {@link UpdateMapper}, а также собственные интерфейсы
 * мапперов, которым не хватило одного источника.
 */
public interface Mapping {
}
